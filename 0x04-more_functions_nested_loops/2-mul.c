#include "main.h"
/*
 * mul - checks if character is upper case
 *
 * @a: the int to multiply
 * @b: the int to multiply
 * Return: Always int (product of a and b)
 */
int mul(int a, int b)
{
int p;
p = a * b;
return (p);
}
