#include "main.h"
/**
 * add - prints the sum of number a and b
 * @a: the numbers to add
 * @b: the numbers to add
 * Return: Always int (sum of a and b)
 */
int add(int a, int b)
{
int c;
c = a + b;
return (c);
}
